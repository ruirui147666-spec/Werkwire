/**
 * Placeholder for the generated Supabase types. Once the project is linked,
 * replace this file with the real output of:
 *
 *   supabase gen types typescript --project-id <ref> > lib/supabase/types.ts
 *
 * Row-shape validation for data coming in/out of the app already happens
 * through the zod schemas in @werkwire/shared — this type only affects
 * autocomplete/typing on the supabase-js client, so a loose fallback here
 * is safe rather than a correctness risk.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
